{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "${description}",
  "authors": [
    "${authors}"
  ],
  "contact": {
    "homepage": "${homepage}",
    "sources": "${sources}",
    "issues": "${issue}"
  },
  "license": "${license}",
  "icon": "assets/${id}/icon.png",
  "environment": "*",
  "entrypoints": {
    "main": [
      "dev.lemonnik.fern_config.FernConfig"
    ]
  },
  "mixins": [
    "fern_config.mixins.json"
  ],

  "depends": {
    "minecraft": "${minecraft_dependency}",
    "java": ">=${java}"
  }
}